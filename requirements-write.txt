# [2] 글쓰기 + [3] 에디터 입력 (로컬에서만 필요)
# 수집만 돌리는 GitHub Actions는 설치하지 않는다.
#
# [3]의 기본 경로인 `python -m src.handoff`는 표준 라이브러리만 쓴다.
# playwright는 구경로(`python -m src.publish`) 전용이다.
-r requirements.txt
anthropic>=1.0.0
playwright>=1.49.0
